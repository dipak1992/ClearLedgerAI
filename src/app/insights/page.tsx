/**
 * Soft alias for `/reports`. Phase 7 will replace the Reports page
 * outright with a redesigned Insights center; for now we simply
 * re-export the existing Reports page so the navigation can point
 * users at the forward-looking URL without breaking the old one.
 */
export { default } from "@/app/reports/page";
